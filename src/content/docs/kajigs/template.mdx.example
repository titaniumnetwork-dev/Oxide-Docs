---
title: title
description: brief description listing required versions
author: author or organization
keywords: ["type", "kernver * / any kernver"]
link: https://github.com/FWSmasher/Cryptosmite
kernver: 2
unenrollment: true (optional field, does not need to be set to false.)
---

{frontmatter.link && (<><a href={frontmatter.link}>{frontmatter.link}</a><br/></>)}
{frontmatter.description}<br/>
{frontmatter.author && <small><i>By {frontmatter.author}</i><br/></small>}

### Requirements
1. any items needed or urls unblocked

### Steps
1. step
2. step with note
    :::note
    note for step 2
    :::
3. step with codeblock
    ```
    code
    ```
4. step with substeps
    - substep